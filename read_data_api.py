import requests
import pandas as pd 

import os

token = os.getenv("API_TOKEN")
print(f"Token : {token}")
#esponse = requests.get("https://jsonplaceholder.typicode.com/users")
#ata = response.json()
#f = pd.DataFrame(data)
#f = df [["id", "name"]]
#rint(df)
